---
title: "Edge Device 양자화 / 모델 경량화 업데이트 (2026-08-06)"
date: 2026-08-06
category: edge-quantization
items:
  - title: "추론 LLM의 PTQ 붕괴 문제 - 사고 과정을 보정 데이터로 쓰는 AYOT 1.58비트 양자화"
    type: "논문"
    summary: "LLM을 PTQ로 1.58비트(터너리)까지 양자화하면 기존 학습 기반 방법(CAT-Q 등)도 수학·코딩처럼 사고 과정이 긴 추론 과제에서 성능이 붕괴하는데, 이는 보정(calibration) 단계에서 모델이 실제로 생성하는 체인 오브 소트(CoT)와 최종 답변을 무시한 채 질문만으로 보정하기 때문이라는 것이 핵심 관찰이다. 저자들은 AYOT(Attend to Your Own Thoughts)라는 간단한 보정 기법을 제안해, 사전학습된 고정밀 모델이 직접 생성한 추론 흔적과 답변을 문맥으로 넣어 터너리화를 수행함으로써 붕괴를 완화하는데, 이는 온디바이스에서 저비트 추론 모델을 돌릴 때 '무엇으로 보정하느냐'가 비트 수 자체만큼 중요할 수 있음을 보여주며, 한계로는 보정 데이터 생성에 고정밀 모델의 추가 추론 비용이 든다는 점이 있다."
    source_url: "https://arxiv.org/abs/2608.01078"
  - title: "KV 캐시를 INT2까지 - 어텐션 출력 오차를 직접 줄이는 회전 기반 양자화(OptR)"
    type: "논문"
    summary: "기존 회전 기반 KV 캐시 양자화 기법들은 캐시 자체의 통계나 어텐션 계산 이전 단계의 대리 오차(proxy error)를 최소화하도록 회전 행렬을 학습했지만, 실제로 모델 출력에 영향을 미치는 것은 어텐션과 출력 투영(W_O)을 모두 거친 뒤의 오차라는 점에 저자들은 주목한다. OptR은 이 post-W_O 어텐션-출력 오차를 키(key) 유발 항과 밸류(value) 유발 항으로 분해하고, INT2 양자화와 어텐션 경로 전체를 관통해 헤드별 직교 보정을 학습함으로써 초저비트(2비트) KV 캐시에서도 정확도 저하를 줄인다. 장문 컨텍스트 추론에서 KV 캐시는 메모리·대역폭의 주요 병목이므로 온디바이스 장문 추론에 직접적인 의미가 있지만, 헤드별 학습된 보정 행렬을 추가로 저장·적용해야 하는 복잡도 증가는 트레이드오프로 남는다."
    source_url: "https://arxiv.org/abs/2608.02691"
  - title: "아키텍처 탐색과 양자화를 따로 하지 말라 - NAS와 혼합정밀도 양자화 공동 최적화"
    type: "논문"
    summary: "기존 LLM 경량화 파이프라인은 대개 NAS(신경망 구조 탐색)로 아키텍처를 먼저 정하고 그 뒤에 양자화를 적용하는 순차적 방식이었는데, 이 논문은 탐색 공간을 제한하지 않고 아키텍처 구성과 선형 레이어별 혼합정밀도 양자화 선택을 하나의 미분 가능한 탐색 과정에서 동시에 최적화하는 프레임워크를 제안한다. 그 결과 동일한 정확도 기준에서 순차적 NAS-후-양자화 베이스라인보다 최대 1.4배 빠른 추론을 얻거나, 동일한 레이턴시 기준에서는 7개 추론 과제 평균 정확도를 최대 6%p 끌어올려 아키텍처와 정밀도 선택이 서로 독립적이지 않음을 보여주지만, 탐색 공간이 커지는 만큼 탐색 자체의 계산 비용이 늘어나는 것은 감내해야 할 대가다."
    source_url: "https://arxiv.org/abs/2606.04063"
  - title: "압축 기법은 따로 놀지 않는다 - 멀티모달 온디바이스에서 토큰 압축·MoE 라우팅·양자화의 상호작용"
    type: "논문"
    summary: "이 서베이는 비전-언어/멀티모달 모델을 엣지에 배포할 때 흔히 쓰이는 시각 토큰 압축, KV 캐시 최적화, MoE 라우팅, 저비트 양자화 같은 기법들이 실무에서는 독립적으로 최적화되고 있지만 실제로는 서로 간섭한다는 점을 지적한다. 예를 들어 시각 토큰 압축은 하위 언어 레이어와 MoE 라우터가 보는 특징 분포 자체를 바꾸고, 이는 라우팅 불안정성과 전문가(expert) 트래픽 변화로 이어지며 다시 양자화 난이도에 영향을 준다는 것이다. 개별 기법의 벤치마크 수치만 보고 조합하면 실제 배포 시 예상치 못한 정확도 저하를 겪을 수 있다는 경고이지만, 상호작용을 정량적으로 함께 최적화하는 구체적 알고리즘 제시보다는 문제 제기와 기존 연구 정리에 가깝다는 한계가 있다."
    source_url: "https://arxiv.org/abs/2607.20981"
  - title: "회전 기반 양자화, 이미지 코덱에도 통할까 - 하다마드 변환으로 헤비테일 텐서 길들이기"
    type: "논문"
    summary: "학습형 이미지 코덱(learned image coding)을 INT8로 양자화하면 압축 효율이 크게 떨어지는데, 이는 LLM에서 흔한 활성값 이상치 문제와 달리 코덱 특유의 무거운 꼬리분포(heavy-tailed) 텐서와 채널 간 큰 편차 때문이라는 게 이 논문의 관찰이다. HaTQ는 LLM 양자화에서 쓰이던 직교 하다마드 재매개변수화를 양자화 이전에 적용해, 각 선형 연산의 원래 함수 매핑은 그대로 유지하면서 가중치와 활성값의 채널 간 분포를 균일 INT8에 더 적합한 형태로 재분배한다. 회전 기반 재매개변수화가 LLM을 넘어 이미지 압축이라는 이질적 도메인에서도 유효함을 보여주지만, 코덱의 비트율-왜곡 성능에 특화된 기법이라 다른 비전 태스크로의 일반화는 별도 검증이 필요하다."
    source_url: "https://arxiv.org/abs/2608.01653"
---
