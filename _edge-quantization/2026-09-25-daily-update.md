---
title: "Edge Device 양자화 / 모델 경량화 업데이트 (2026-09-25)"
date: 2026-09-25
category: edge-quantization
items:
  - title: "압축 기법은 순서가 알고리즘 자체보다 더 중요할 수 있다 — Prune-Quantize-Distill 파이프라인 실증 연구"
    type: "논문"
    summary: "가지치기(pruning)·INT8 QAT·지식증류(KD)를 결합할 때 무엇을 먼저 적용하느냐가 결과를 크게 좌우한다는 실증 연구다. 핵심 통찰은 파라미터 수나 FLOPs 같은 압축 프록시가 실제 CPU 벽시계(wall-clock) 추론 시간을 신뢰성 있게 예측하지 못한다는 것 — 비구조적(unstructured) 스파시티는 모델 저장 용량은 줄이지만, 희소 커널의 불규칙한 메모리 접근 오버헤드 때문에 표준 CPU에서는 오히려 속도가 느려지거나 거의 개선되지 않을 수 있다. 압축 기법을 단품으로 비교하는 논문은 많지만 '어떤 순서로 조합해야 실제 배포 환경에서 이득을 보는가'를 체계적으로 다룬 연구는 드물어, 엣지 배포 파이프라인을 설계하는 실무자에게 실질적 지침을 준다."
    source_url: "https://arxiv.org/abs/2604.04988"
  - title: "NAS가 찾은 '최적 구조'가 양자화 후에도 최적일까 — INT4 PTQ가 NAS 파레토 공간을 흔드는 방식"
    type: "논문"
    summary: "하드웨어 인지형 NAS(Neural Architecture Search)는 FP32 기준으로 정확도-효율 파레토 최적 구조를 찾지만, 실제 배포에서는 그 구조가 다시 양자화된다 — 그런데 PTQ가 이 파레토 구조 자체를 어떻게 왜곡시키는지는 그동안 거의 규명되지 않았다. 이 연구는 NAS-Bench-201의 15,625개 전체 아키텍처에 대해 INT4 PTQ가 파레토 순위를 얼마나 교란하는지 정량적 안정성 지표로 측정하고, INT4로 별도 학습된 서로게이트(surrogate)보다 오히려 FP32 제로샷 서로게이트가 더 넓은 파레토 공간 커버리지를 보인다는 반직관적 결과를 제시한다. NAS와 양자화를 따로 다루는 연구가 대부분인 상황에서 두 단계를 하나의 공동 설계 문제로 봐야 한다는 근거를 데이터로 보여준다."
    source_url: "https://arxiv.org/abs/2608.13293"
  - title: "이미지 CNN용 PTQ 공식이 그래프에서는 왜 깨지는가 — 토폴로지 인지형 GNN 양자화 TopGQ"
    type: "논문"
    summary: "GNN(그래프 신경망)은 자율주행의 궤적 예측·장면 그래프·포인트클라우드 관계 모델링 등에 널리 쓰이지만, 이미지 도메인에서 검증된 PTQ 기법을 그대로 적용하면 메시지 패싱·집계(aggregation) 연산의 비유클리드적 구조 때문에 느리고 부정확해진다. DAC 2026에 채택된 TopGQ는 활성화 양자화 스케일을 인접행렬(adjacency matrix)에 흡수시키는 이중축 스케일 흡수(dual-axis scale absorption)와, 노드의 지역 토폴로지를 프록시로 삼아 비슷한 구조의 노드를 그룹화해 양자화하는 TopPIN을 결합해 정확도 손실 없이 양자화 소요 시간을 한 자릿수 단위로 단축한다. 이미지가 아닌 그래프·관계형 데이터에서 양자화를 어떻게 재설계해야 하는지 보여주는 사례로, 비전 중심 PTQ 지식만으로는 놓치기 쉬운 지점을 짚는다."
    source_url: "https://arxiv.org/abs/2608.30394"
  - title: "양자화 저항성은 옵티마이저 선택에서 갈린다 — 아웃라이어 지표가 PTQ 성능을 예측하지 못하는 이유"
    type: "논문"
    summary: "양자화 정확도 손실을 논할 때 흔히 활성화·가중치의 아웃라이어 분포를 원인으로 지목하지만, 이 연구는 50M~1.5B 규모 모델을 6종의 옵티마이저로 정밀 튜닝해 학습시킨 뒤 PTQ·QAT 강건성을 비교한 결과 max-to-mean ratio(MMR)나 Kurtosis 같은 표준 아웃라이어 지표가 옵티마이저 간 PTQ 성능 차이를 전혀 예측하지 못한다는 것을 보여준다. 대신 Shampoo 옵티마이저로 학습한 모델이 가장 낮은 정확도 저하와 가장 높은 파라미터 효율을 보였는데, 이는 양자화 친화적 모델을 만들려면 학습 후 보정(calibration)보다 학습 알고리즘 자체의 선택이 더 근본적인 변수일 수 있다는 뜻이다. 양자화 손실을 캘리브레이션 기법만으로 해결하려는 접근의 한계를 짚는 개념적 반박 연구다."
    source_url: "https://arxiv.org/abs/2509.23500"
  - title: "DVFS 거버너가 놓치고 있던 변수 — CPU/GPU 클럭만 보고 메모리 클럭(EMC)을 무시하면 생기는 지연시간 붕괴"
    type: "논문"
    summary: "데드라인 인지형 DVFS 거버너들은 지연시간을 CPU·GPU 클럭의 함수로만 모델링하고 메모리 클럭(EMC) 상태는 조건에 넣지 않는데, 이 연구는 Jetson Orin 실측에서 EMC를 무시한 GPU 전용 지연시간 추정기가 타이트한 데드라인에서 사이클의 25~28%를 놓치는 반면 EMC를 함께 모델링한 2셀 접근은 2% QoS 예산 하에서 미스율을 0.9% 이하로 억제한다는 것을 보인다. 더 나아가 긴 응답에서는 KV 캐시 증가로 데드라인 충족 가능성이 잠식되는 디코드 구간(decode horizon)과, GPU를 다른 프로세스와 공유할 때 큐잉 지연 꼬리가 벌어지는 co-tenancy까지 상태 변수로 포함해야 한다고 지적한다. 양자화·경량화로 연산량을 줄여도 전력·스케줄링 계층의 블라인드 스팟 때문에 실제 온디바이스 지연시간 SLA가 무너질 수 있음을 보여주는, 엣지 추론 최적화의 숨은 변수를 짚는 연구다."
    source_url: "https://arxiv.org/abs/2606.16106"
---
