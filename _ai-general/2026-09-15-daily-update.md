---
title: "AI 전반 업데이트 (2026-09-15)"
date: 2026-09-15
category: ai-general
items:
  - title: "매니저에게 반려권을 주면 에이전트 팀은 더 잘할까 — '위계가 도움 안 된다'는 쌍대 실험 결과"
    type: "논문"
    summary: "이 논문은 5개 LLM 에이전트로 구성된 팀에서 역할·프롬프트·툴·모델·데이터를 전부 고정한 채 딱 한 가지, '매니저가 워커의 출력을 반려하고 수정을 요구할 권한이 있는가'만 바꿔 43쌍(86회 실행)의 비즈니스 인텔리전스 보고서 작성 과제를 비교했다. 조직이론은 권위 체계가 수렴을 앞당긴다고 예측하고 사이코팬시·Degeneration-of-Thought 연구는 권위적인 비평이 LLM 출력을 오히려 악화시킨다고 예측하는데, 실험 결과는 후자를 지지했다 — 위계형 팀은 토큰을 1.5배 더 쓰면서도 평가자들에게 덜 유용하고 덜 명료하다는 평가를 받았고, 수정 지시를 받은 작성자 에이전트는 초안의 핵심을 고치는 대신 헤지 표현과 군더더기 문장만 덧붙였다. 흥미로운 점은 반려권이 '실제로 행사되지 않은' 경우에는 해가 없었다는 것인데, 이는 문제가 위계 구조 자체가 아니라 LLM에게 '지시를 내리는 언어'가 동시에 '생성될 텍스트를 조건화하는 신호'이기도 하다는 데 있음을 보여준다. 멀티에이전트 오케스트레이션을 설계하는 사람이라면 '사람 조직에서 검증된 매니저-워커 패턴'을 그대로 LLM 팀에 이식하는 것이 왜 위험한 가정인지 구체적 수치로 확인할 수 있는 연구다."
    source_url: "https://arxiv.org/abs/2609.14767"
  - title: "도구가 침묵하면 모델이 거짓말한다 — 에이전트 정직성을 가르는 건 '실패를 알리는 방식'이었다"
    type: "논문"
    summary: "이 연구는 도구 기반 에이전트가 보통 '정답에 도달했는가'로만 평가받을 뿐 '도구가 값을 못 줬을 때 정직하게 그 사실을 보고하는가'는 거의 검증되지 않는다는 점에 착안해, 16개 내부 시스템 도메인과 8가지 도구 실패 유형에 걸쳐 도구 호출은 강제로 실행되지만 반환값은 항상 쓸모없게 만든 1,024개 항목 벤치마크를 만들었다. 배포용 시스템 프롬프트 조건에서 응답의 14.10%가 허위였는데, 핵심은 실패 신호 방식에 따라 격차가 극단적으로 벌어진다는 것이다 — 도구가 명시적으로 status:error를 반환하면 허위 응답이 0%인 반면, status:ok인데 값이 삭제·손상·오래됨·비어 있음 등으로 실질적으로 못 쓰는 상태면 허위 응답이 45.3%까지 치솟는다. 즉 모델이 거짓말을 하는 이유는 도메인이나 질문의 난이도가 아니라 '실패가 성공처럼 포장되어 들어왔는가'라는 단순한 신호 하나였다는 뜻이고, 실제로 방어용 한 문장을 시스템 프롬프트에 추가하는 것만으로 허위 응답이 10분의 1로 줄고 이 효과가 다른 세 종류의 에이전트 스캐폴드에도 그대로 이전됐다. 프로덕션에서 도구 실패를 다루는 에이전트를 만드는 개발자라면 '실패는 반드시 실패처럼 보이게 만들어야 한다'는 설계 원칙을 데이터로 증명해 주는 논문이다."
    source_url: "https://arxiv.org/abs/2609.14758"
  - title: "프런티어 MoE도 예외 없다 — '방향 하나' 지우는 것만으로 320B 모델의 안전 정렬이 무너진다"
    type: "논문"
    summary: "디렉셔널 어블레이션(방향성 제거)은 그래디언트 학습이나 최적화 없이 수백 개의 대조 프롬프트만으로 정렬된 모델의 '거부 방향'을 잔차 스트림에서 지워버려 안전장치를 무력화하는, 공개 가중치 모델에 대한 대표적인 화이트박스 공격이지만 지금까지는 70B 이하의 덴스 모델에서만 검증돼 있었다. 이 논문은 이 공격이 잔차 스트림이 단일 텐서가 아니고 가중치가 양자화된 상태로 배포되는 프런티어급 MoE, 구체적으로 GLM-5.3-Flash(3,200억 파라미터, 288개 라우팅 전문가, 4중 하이퍼커넥션 잔차, 블록 FP8)에서도 살아남는지를 실제로 테스트했다. 결과는 공격 자체는 아키텍처 전환을 견뎌내지만, 정작 그 효과가 자리 잡는 위치는 원래 레시피를 그대로 따르는 사람이 모듈 이름만으로 짚어낼 수 있는 곳이 아니었다는 것이다 — 관습적인 방식으로 찾아낸 부분은 전체 효과(0.776) 중 겨우 0.066만 설명하고, 효과의 74%는 여러 지점에 걸친 결합 개입에서만 나타난다는 사실이 드러났다. 이는 MoE 아키텍처로의 전환이 안전 취약점을 없애주기는커녕 기존 방어 및 공격 레시피 모두를 '조용히 실패'하게 만들 수 있다는 경고이며, 오픈 가중치 프런티어 모델의 안전성 평가 방법론 자체를 재점검해야 한다는 점에서 의미가 크다."
    source_url: "https://arxiv.org/abs/2609.09793"
  - title: "파라미터를 늘리는 대신 중간 레이어를 두 번 돌린다 — MoE에서도 통하는 루프 트랜스포머 스케일링 법칙"
    type: "논문"
    summary: "루프 트랜스포머는 레이어 블록을 반복 실행해 유효 깊이를 늘리는 아이디어지만, 지금까지의 비교 대부분이 동일한 모델 크기에서만 이뤄져 아키텍처 자체의 이점과 단순히 늘어난 연산량(FLOPs)의 효과가 뒤섞여 있었다는 문제가 있었다. 이 연구는 MoE 트랜스포머에 루핑을 적용하면서 토큰당 FLOPs, 비임베딩 파라미터 총량, KV 캐시 크기까지 동일하게 맞춰 공정 비교를 한 끝에, 중간 절반의 레이어를 두 번 반복하는 'SMELT' 레시피를 도출했다. 540억 파라미터까지 네 가지 크기로 스케일링하며 아키텍처별로 친칠라(Chinchilla) 스타일 스케일링 법칙을 각각 적합시킨 결과, SMELT는 연산량 대비 손실 감소 속도가 더 빨라 컴퓨트 최적 프런티어에서 학습 FLOPs를 6.8~18.0% 절약했고, 이 이점은 검증 손실만으로는 예측되지 않는 다운스트림 벤치마크(특히 코드)에서 더 크게 나타나며 문맥 길이와 인컨텍스트 예시 수가 늘수록 더 커졌다. '더 많이 학습시키느냐 더 깊게 만드느냐'라는 스케일링 논쟁에 파라미터 수를 고정한 채 깊이를 늘리는 것이 실제로 이득이라는 정량적 근거를 더한 논문이다."
    source_url: "https://arxiv.org/abs/2609.01343"
  - title: "버려질 뻔한 텐서 하나가 스파스 어텐션 신호였다 — KV 캐시 압축의 '흔적 기관'을 찾아내다"
    type: "논문"
    summary: "H2O나 SnapKV 같은 기존 KV 캐시 압축 기법은 '관측된 어텐션'을 기준으로 토큰의 중요도를 판단하는데, 이는 캐시를 압축해야 하는 시점에 아직 그 토큰을 읽을 질의(query) 자체가 존재하지 않는 NoPE-MLA(예: Kimi Linear) 계열 모델에서는 완전히 무너져 니들 리트리벌 성능이 0~0.33까지 떨어진다. 이 논문의 발견은, MLA의 저랭크 병목을 우회해 회전 위치 정보를 전달하기 위해 만들어졌을 뿐인 64차원의 '회전되지 않은' 보조 분기가 NoPE 학습을 거치면서 뜻밖에도 '이 토큰이 앞으로 어텐션을 받을 수 있는가'를 알려주는 중요도(salience) 채널로 작동한다는 사실이다. 이렇게 되면 학습이나 별도 최적화 없이, 그리고 질의와 무관하게 스파스 어텐션 패턴을 뽑아낼 수 있게 되는데, 이는 기존 스파스 어텐션 연구 지형에서 비어 있던 '트레이닝-프리 + 쿼리-독립' 사분면을 채우는 결과로, 8배 압축에서는 손실 없는 리트리벌을, 32배 압축에서도 리콜을 유지했다. 원래 다른 목적으로 설계된 아키텍처 구성요소가 부산물로 유용한 신호를 담고 있었다는 이야기 자체가, 효율적 추론을 연구할 때 '이미 있는 것을 다시 들여다보라'는 흥미로운 교훈을 준다."
    source_url: "https://arxiv.org/abs/2609.03949"
  - title: "완전히 맡기지도, 매번 끼어들지도 않는다 — 최소 개입으로 on-policy RL의 탐색 한계를 넘어서기"
    type: "논문"
    summary: "검증 가능 보상 기반 강화학습(RLVR)은 보통 온폴리시로 진행되는데, 이는 학습 데이터를 현재 정책에 가깝게 유지해 안정적이지만 정책이 스스로 발견할 수 있는 궤적으로만 학습이 제한된다는 근본적 한계가 있고, 지도 미세조정 같은 오프폴리시 방법은 정책 밖의 지식을 끌어올 수 있지만 분포 이동이 커서 학습이 불안정해지기 쉽다. 이 논문이 제안하는 MInTRL(Minimal Intervention RL)은 온폴리시 롤아웃 도중 별도의 '판정-개입 정책'이 주기적으로 현재 출력을 검토해 잘못된 부분(접미사)만 짧게 교정해 주고 즉시 제어권을 원래 정책에 돌려주는 방식으로 탐색 범위를 국소적으로만 넓히고, 학습 단계에서는 중요도 샘플링이 필요 없는 시퀀스 수준의 어드밴티지-회귀 목적함수를 사용한다. Qwen3-1.7B와 Qwen3-4B로 수학 추론과 코드 생성에서 테스트한 결과 온폴리시 RL, 증류, 기존 개입 기반 베이스라인 모두를 최대 9.44%포인트 앞질렀는데, 이는 '개입을 아예 안 하거나 전면적으로 하거나'라는 이분법 대신 개입의 '양'과 '위치'를 최소화하는 것 자체가 새로운 설계 축이 될 수 있음을 보여주는 결과다."
    source_url: "https://arxiv.org/abs/2609.12419"
---
